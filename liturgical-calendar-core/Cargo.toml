[package]
name = "liturgical-calendar-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"

[dependencies]
sha2 = { version = "0.10", default-features = false }

[features]
gen-headers = []
